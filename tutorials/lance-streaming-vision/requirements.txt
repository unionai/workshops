# Lance + Flyte streaming vision tutorial.
#
# Default torch/torchvision wheels include CUDA, so the same image runs the data
# tasks on CPU nodes and the detector training on GPU (T4) nodes.

flyte[tui]>=2.0
# flyteplugins-lance isn't on PyPI yet — install it from the plugin PR
# (flyteorg/flyte-sdk#1313), pinned to its head commit, out of the plugins/lance subdir.
flyteplugins-lance @ git+https://github.com/flyteorg/flyte-sdk.git@8dcfc882a7cd3f66bc8efc600cebb594893eb219#subdirectory=plugins/lance
pylance>=0.19.0
pyarrow>=17.0.0
pillow>=10.0.0
numpy
torch>=2.4.0
torchvision>=0.19.0
torchmetrics>=1.4.0
pycocotools>=2.0.7
datasets>=2.19.0
